# URL of the existing FastAPI backend (admin_api). Used by next.config rewrites.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-openssl-rand-hex-32
